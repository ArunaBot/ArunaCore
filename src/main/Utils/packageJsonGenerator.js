class PackageJsonGenerator {
    constructor () {
    }

    gen(object, dir) {
        return new Promise((resolve, reject) => {
            
        });    
    }
}

module.exports = PackageJsonGenerator;